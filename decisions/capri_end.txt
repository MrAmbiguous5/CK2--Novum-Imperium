decisions = {

	end_capri = {
		is_high_prio = yes		
		potential = {
			trait = capri_seclusion
			had_character_flag = { flag = decided_capri days = 365 }
		}
		allow = {
			NOT = { trait = depressed }
			NOT = { trait = stressed }
		}
		effect = {
			character_event = { id = toe.1718 }
		}
		ai_will_do = {
			factor = 1
		}
	}
}